{
  "name": "vakeelsathi-leads",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "db:init": "wrangler d1 execute vakeelsathi_leads --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20250906.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "^4.0.0"
  }
}
